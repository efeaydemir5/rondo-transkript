Rondo Transkript is a Python tool I built to convert MusicXML files into structured note transcripts. I originally made it to represent music programmatically inside a Roblox game instead of writing note sequences manually. The project parses MusicXML and handles playback rules such as repeats and endings, then outputs timing, duration, and pitch data that can be used by a script.
